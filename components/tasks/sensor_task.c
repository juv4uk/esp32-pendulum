#include "tasks.h"

// TODO: while(1) { read AS5600, hand the sample off to processing_task
// (queue, not a shared global you both poke at), vTaskDelay(...) }
void task_sensor(void *pvParameters)
{
}
